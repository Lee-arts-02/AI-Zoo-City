/**
 * Shared narrative text cadence — keep in sync with home intro and Step 4 robot dialogue.
 * @see components/home/HomePageContent.tsx
 */
export const HOME_CHAR_MS = 40;
export const HOME_BETWEEN_SENTENCE_MS = 850;

/** Slower, more readable typing for Step 4 robot (learner-paced; not tied to home intro speed). */
export const STEP4_CHAR_MS = 60;

/** Pause between `//` auto-chained segments in the same bubble (ms). */
export const STEP4_BETWEEN_AUTO_SEGMENTS_MS = 650;
